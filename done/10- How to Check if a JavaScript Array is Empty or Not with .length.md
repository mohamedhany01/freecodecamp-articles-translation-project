# كيف تتحقق ما اذا كانت مصفوفة فارغة ام لا فى الجافا سكريبت بأستخدام خاصية الطول

عند استخدامك للغة الجافا سكريبت "JavaScript"، بعض الاحيان تحتاج الى معرفة هل مصفوفة "array" ما خالية ام لا.

للتحقق من ان مصفوفة ما خالية ام لا، يمكن استخدام خاصية متوفرة مسبقا فى الجافا سكريبت.


خاصية "property" طول المصفوفة "length" ترجع لنا عدد العناصر الموجودة فى المصفوفة. وذلك بمعرفة عدد العناصر الموجودة فى المصفوفة، وبالطبع المصفوفة الخالية ترجع لنا `0`.

والان لنجرب بعض الامثلة لتوضيح الفكرة.

## طريقة الكتابة
```javascript
Const myArray = [‘Horses’, ‘Dogs’, ‘Cats’];
```

قمنا هنا، بإنشاء مصفوفة تحتوى على ثلاث عناصر.

بأستخدام خاصية الطول "length"، يمكننا التحقق من طول المصفوفة:

```javascript
myArray.length
```

بالطبع سوف ترجع لنا الخاصية 3، لان المصفوفة تحتوى على 3 عناصر فقط.

للتحقق من أن المصفوفة خالية ام لا بأستخدام خاصية الطول، يمكن فعلها بثلاث طرق.

### الطريقة الاولى
اولا، لنقم بإنشاء مصفوفة جديدة خالية تماما من العناصر.

```javascript
const arr = []
```

يمكن التحقق من خلو المصفوفة بأستخدام خاصية `length`

```javascript
arr.length
```

سوف يرجع لنا هذا الكود `0`، لان المصفوفة لا تحتوى على اى عناصر.

### الطريقة الثانية 
 يمكن ايضا بشكل صريح/واضح التحقق ما اذا كانت مصفوفة ما خالية ام لا، بأستخدام الكود التالي:
 
`if (arr.length === 0) { console.log("Array is empty!") }`

اذا كانت المصفوفة خالية فعلا، سوف تظهر الرسالة السابقة. اما اذا كانت المصفوفة تحتوى على عناصر بداخلها، الكود بداخل `if` الشرطية لن يتم تنفيذه. 


يوجد طريقة ثالثة للتحقق من خلو المصفوفة من العناصر بأستخدام خاصية الطول `length`.

### الطريقة الثالثة 

يمكن دمج خاصية الطول "length" و معامل "operator" النفي "not" المنطقي "logical" فى لغة الجافا سكريبت، والذى يرمز له بــ "!"، لتحقق من خلو المصفوفة او العكس.

معامل النفي `!` يقوم بنفي تعبير/عبارة برمجي(ة) "expression". وبالتالي يمكن إرجاع "return" قيمة صحيحة `true` اذا كانت المصفوفة فارغة. 

على سبيل المثال، دعنا نقوم بفتح لوحة اوامر "console" الجافا سكريبت فى متصفح الانترنت. لفتح لوحة الاوامر فى متصفحك وليكن على سبيل المثال "كروم"، توجه الى الفاحص "Inpsect" ثم لوحة الاوامر "console".

`الفاحص "Inpsect" => لوحة الاوامر "console" `


اولا، لنقم بإنشاء مصفوفة فارغة، لا تحتوى اى عناصر بداخلها.

![](https://www.freecodecamp.org/news/content/images/2020/10/image.png)

ثم، لنستحدم معامل النفى المنطقي "not"، بالاضافة لخاصية الطول "length"، للتحقق من خلو المصفوفة من العناصر او العكس.

![](https://www.freecodecamp.org/news/content/images/2020/10/Screen-Shot-2020-09-30-at-5.29.35-PM.png)

اذا لم نستخدم معامل النفى "not"، الكود `arr.length` سوف يرجع `0` فقط بدونه. لكن عند اضافة المعامل "!"، سوف يرجع لنا قيمة `true` لان قيمة المعامل الاصلية هى `false`. وبالتالى يمكن استنتاج ان قيمة الكود `arr.length` هى `0` اى انه منطقيا يساوى `false`وبالتالى عندى استخدام معامل النفي  `!` ستكون النتيجة النهائية هى `true`. 


لان دعنا نستخدم جملة `if` الشرطية، ونقوم بطباعة رسالة اذا كانت المصفوفة خالية.

![](https://www.freecodecamp.org/news/content/images/2020/10/image-2.png)

عند التحقق من خلو المصفوفة، او العكس، فمن المفضل التحقق اذا كان المتغير فعلا مصفوفة ام لا.

لماذا؟

لان من الممكن ان نكون على اقتناع تمام بأن المتغير هو بالفعل مصفوفة وتملك طول "length"، لكن بدلا من ذلك يكون نوع المتغير سلسة نصية "string": 

![](https://www.freecodecamp.org/news/content/images/2020/10/image-7.png)

ولان يمكن استخدام خاصية الطول `length property` فى انواع بيانات "data types" اخري، فمن المستحسن التحقق من ان نوع البيانات فعلا مصفوفة وليس شئ اخر. 

توفر الجافا سكريبت دالة "method" لتأكد من ان نوع البيانات بالفعل مصفوفة. حيث ترجع هذه الدالة `()Array.isArray`  قيمة `true`عند تمرير مصفوفة لها، و ترجع قيمة `false` عند تمرير شئ اخر. 

دعنا الان نضيف هذه الدالة للمثال السابق.

### كيفية استخدام الدالة ()Array.isArray
![](https://www.freecodecamp.org/news/content/images/2020/10/image-3.png)

## تلخص وخاتمة
فى هذا المقال، تعلمنا كيفية استخدام خاصية الطول `length` فى  الجافا سكريبت فى امثلة متعددة لتحقق من خلو المصفوفة او لا. خاصية الطول `length` ترجع عدد عناصر المصفوفة.

تعلمنا ايضا انه من المفضل استخدام دالة `()Array.isArray` عند استخدام خاصية الطول `length`، للتحقق ما اذا كان نوع البيانات بالفعل مصفوفة ام لا. 







